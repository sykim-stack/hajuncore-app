'use client';
// EMERGENCY: full file too large for single tool call — use local restore
export default function RoomPage() {
  return (
    <div style={{ padding: 24, color: '#fff', background: '#0D1117', minHeight: '100vh' }}>
      <h1>방 페이지 복구 중</h1>
      <p>page.tsx 복구가 필요합니다. artifacts/hajuncore-app-patches/room_page.tsx 를 적용하세요.</p>
    </div>
  );
}

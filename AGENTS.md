<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

## HajunAI 협업 규칙

- 작업 시작 전에 루트 `HANDOFF.md`와 관련 `docs/` 확정 문서를 먼저 읽는다.
- 기준 브랜치는 `master`다. 작업 전 `git fetch origin`과 `git pull --ff-only origin master`로 동기화한다.
- 중단 전에는 `HANDOFF.md`에 작업·결정·검증·다음 작업을 기록한다.
- 상품검증의 공식 원본은 `hajun_messages`다. 기존 `hajun_posts`는 레거시 참고용 읽기 자료이며 신규 상품 원문을 저장하지 않는다.
- 설계 미결 상태에서 별도 상품 원본 테이블, 상품별 방, 자동 승인, 자동 복제를 추가하지 않는다.
